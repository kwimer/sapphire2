<%= link_to "New #{resource_class.model_name.human}", new_resource_path, class: "btn btn-sm btn-primary float-right mt-4" if self.controller.respond_to?(:new) %>
<div class="page-header row no-gutters py-4">
  <div class="col-12 col-sm-4 text-center text-sm-left mb-0">
    <span class="text-uppercase page-subtitle"><%= page_subtitle %>&nbsp;</span>
    <h3 class="page-title"><%= page_title %></h3>
  </div>
</div>
<%= simple_form_for resource, url: form_resource_path do |f| %>
  <%= f.input :summary, as: :text, placeholder: resource.tmdb_summary %>
  <%= f.input :detail, as: :text %>
  <%= f.submit %>
<% end %>
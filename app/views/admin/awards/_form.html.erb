<%= simple_form_for resource, url: form_resource_path do |f| %>
  <div class="card card-small mb-3">
    <div class="card-body">
      <%= f.input :year %>
      <%= f.input :media_id, input_html: {data: {url: search_admin_movies_path}} %>
      <%= f.hidden_field :media_type, value: 'Movie' %>
      <%= f.input :award_type, collection: %w(nominee winner) %>
      <%= f.input :award_name %>
    </div>
    <div class="card-footer border-top">
      <%= f.submit class: "btn btn-sm btn-accent ml-auto d-table" %>
    </div>
  </div>
<% end %>
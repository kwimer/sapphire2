<%= link_to "New #{resource_class.model_name.human}", new_resource_path, class: "btn btn-sm btn-primary float-right" if self.controller.respond_to?(:new) %>
<h1>
  <% if parent %>
    <%= link_to '«', admin_categories_path %><%= parent.name %> <%= resource_class.model_name.human.pluralize %>
  <% else %>
    <%= resource_class.model_name.human.pluralize %>
  <% end %>
</h1>